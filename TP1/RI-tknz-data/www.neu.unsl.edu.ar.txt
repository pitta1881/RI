inicio .lbloading { background: #fff   #lbprevlink:hover
{ background: transparent   #lbnextlink:hover {
background: transparent   #lbcloselink { display:
block; float: right; width: 66px; height: 22px; background: transparent  
/******************************************************************************************** * pageear advertising cornerad by
webpicasso media * leave copyright notice. * * @copyright www.webpicasso.de
* @author christian harz
*********************************************************************************************/ /* * konfiguration /
configuration */ // url to small image   // url to small pageear swf   // url to big image  
// movement speed of small pageear     // mirror  
  // url to open
on pageear click   //
opens pageear     // set direction of
pageear in left or right top browser   setdirection = 'rt'; //add by remush
  /* *
do not change anything after this line */ // flash
check vars   // copyright   // size small peel   // size big peel     // get - params   queryparams +=  ; queryparams +=  ; queryparams +=
 ; queryparams +=  ; queryparams +=  ; queryparams +=  ;
queryparams +=  ;    
  { // get installed flashversion   // check direction     // write div
layer for big swf  ; // check if flash exists/ version matched  
{  
else { // otherwise do nothing or write message ...
  // close div
layer for big swf  ; // write div layer for
small swf  ; //
check if flash exists/ version matched   {   else {
// otherwise do nothing or write message ...     //added by remush
//for set cookies   {   {     while   c =  ; if   return   return null; } //end added
by remush  ;  {  { //if
you choose behaviour once, you must enable cookies on your
browser cookie =  ; //if not ie4+ nor ns6+  { document.cookie="testcookie";     {  ;
  else{   webmaster l&g
solutions